# Copy to .env.local for local development. Never commit runtime secrets.
SERVER_HOST=0.0.0.0
SERVER_PORT=3000
# Optional browser API origin. Leave empty to use the Vite /api proxy in development.
VITE_API_BASE_URL=
# Optional local proxy target; defaults to http://127.0.0.1:13002.
VITE_API_PROXY_TARGET=http://127.0.0.1:13002
# Explicit local-only opt-in for the development admin API token. This value
# is embedded in browser assets; never enable it for production or commit a real token.
VITE_LOCAL_DEV=false
VITE_ADMIN_API_TOKEN=
DATABASE_URL=
AI_PROVIDER_URL=
# Optional capability-specific override; takes precedence over AI_PROVIDER_URL.
AI_ARTICLE_SCORING_1_URL=
AI_PROVIDER_API_KEY=
AI_PROVIDER_MODEL=
# Keep legacy for existing capability endpoints; use openai for LiteLLM/OpenAI-compatible /v1/chat/completions.
AI_PROVIDER_PROTOCOL=legacy
# Maximum time for a provider HTTP request when no caller signal is supplied.
AI_PROVIDER_TIMEOUT_MS=600000
DB_POOL_MAX=10
LOG_REQUEST_BODY=false
LOG_RESPONSE_BODY=false
